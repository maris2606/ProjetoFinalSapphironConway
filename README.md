# ProjetoFinalSapphironConway
Para compilar o projeto,  utilize o compilador g++ com suporte ao padrão C++11 ou superior,  incluindo todos os arquivos .cpp do diretório: g++ main.cpp mecanicas/*.cpp -Iheaders -std=c++17 -o jogo
